#include <stdio.h>

int max(a,b)
{
	if(a>b)
	{return a;
	}
	else return b;
}

int main()
{
	int x,y,z,w,t1,t2,t3,t4,t5;
	scanf("%d %d %d %d",&x,&y,&z,&w);
	
	t1=max(x,y);
	t2=max(z,w);
	t3=max(t1,t2);
	t4=max()

 } 
